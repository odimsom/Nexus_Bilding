Square icon-only button for toolbars and row actions; always pass `label` for accessibility.

```jsx
<IconButton label="More actions"><i data-lucide="ellipsis" /></IconButton>
<IconButton label="Edit" size="sm"><i data-lucide="pencil" /></IconButton>
```
