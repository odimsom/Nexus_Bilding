Styled native select. Pass `options` or `<option>` children.

```jsx
<Select label="Document type" options={['Invoice','Credit Memo','Quote']} />
<Select label="Location" placeholder="Choose…" options={[{value:'SD',label:'Santo Domingo'},{value:'STI',label:'Santiago'}]} />
```
