Centered empty/zero-results placeholder with optional action.

```jsx
<EmptyState
  icon={<i data-lucide="file-text" />}
  title="No open invoices"
  action={<Button variant="primary">New invoice</Button>}>
  Documents you create will appear here.
</EmptyState>
```
