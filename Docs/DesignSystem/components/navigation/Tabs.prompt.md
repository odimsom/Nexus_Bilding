Underline tabs with optional count pills. Controlled via `value`/`onChange`.

```jsx
<Tabs value={tab} onChange={setTab} tabs={[
  { id: 'all', label: 'All', count: 142 },
  { id: 'open', label: 'Open', count: 18 },
  { id: 'posted', label: 'Posted', count: 124 },
]} />
```
